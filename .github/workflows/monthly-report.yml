name: Monthly CRM Report

on:
  # 1st of every month at 06:00 UAE time (UTC+4) = 02:00 UTC
  schedule:
    - cron: '0 2 1 * *'

  # Allow manual trigger from GitHub Actions tab
  workflow_dispatch:

jobs:
  generate-monthly-report:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      # canvas requires native build tools
      - name: Install system dependencies for canvas
        run: |
          sudo apt-get update -q
          sudo apt-get install -y -q \
            build-essential \
            libcairo2-dev \
            libpango1.0-dev \
            libjpeg-dev \
            libgif-dev \
            librsvg2-dev

      - name: Install Node dependencies
        run: npm ci

      - name: Generate Monthly Report & Upload to Dropbox
        env:
          SB_URL: ${{ secrets.SB_URL }}
          SB_KEY: ${{ secrets.SB_KEY }}
          DROPBOX_ACCESS_TOKEN: ${{ secrets.DROPBOX_ACCESS_TOKEN }}
        run: node scripts/generate-report.js monthly

      - name: Report result
        if: always()
        run: |
          echo "Monthly report job finished with status: ${{ job.status }}"
